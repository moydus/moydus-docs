# Cloudflare Workers AI endpoint (your deployed worker URL)
# Set this in Cloudflare Pages dashboard → Settings → Environment Variables
PUBLIC_AI_API_URL=https://your-worker.your-subdomain.workers.dev
